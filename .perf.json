{
	"binary": "temp",
	"baseline": "main",
	"config": {
		"warmups": 3,
		"duration": 5,
		"allowFailures": true,
		"commands": [{ "name": "sleep", "command": "sleep 0.1" }]
	},
	"output": {
		"branch": "gh-pages",
		"json": "benchmarks.json",
		"html": "benchmarks.html",
		"limit": -1
	}
}
